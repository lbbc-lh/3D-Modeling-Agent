# 项目源码构造分析

## 1. 项目定位

这个项目本质上是一个 **LLM 驱动的任务型对话 Agent**，但不是“单体式大模型问答程序”，而是拆成了多个独立服务：

- `entry-service`：入口编排层，负责接收请求、维护上下文、并发调用下游服务、回传结果。
- `intent-service`：意图召回服务，使用本地分类模型做 TopK 意图预测。
- `reject-service`：拒识服务，判断当前输入是否应该被系统拒绝或走兜底逻辑。
- `chatnlu-service`：NLU 服务，结合意图召回结果和 LLM function calling 抽取槽位。
- `async-consumer`：异步任务消费者，负责消费 RabbitMQ 中的日志任务并落盘。
- `Redis`：保存短期会话态、仲裁历史、改写历史、闲聊历史。
- `RabbitMQ`：把非核心链路的日志写入动作异步化。

从工程思路看，这个项目不是追求“模型最强”，而是追求：

- 把一次对话拆成若干可独立演进的能力模块；
- 让入口服务只做编排，不把所有推理逻辑塞进一个进程；
- 用 Redis 保存短时上下文，让多轮对话有连续性；
- 用 RabbitMQ 把日志/可观测性任务从主链路剥离，降低阻塞风险；
- 用 Docker Compose 一键拉起整条链路，便于演示、部署和面试展示。

## 2. 总体架构

README 里的架构图和代码实现一致，主结构如下：

```text
Client(dialog.py / SocketIO)
        |
        v
 entry-service (start.py)
   |        |        |        |
   |        |        |        +--> Redis
   |        |        +------------> RabbitMQ -> async-consumer
   |        +---------------------> reject-service
   +------------------------------> chatnlu-service -> intent-service
                    \
                     \--> 外部 LLM / Bot / 重写 / 仲裁 / 相关性判定
```

关键特征：

- **入口层是调度中心**，不是算法中心。
- **NLU 和分类模型被拆成独立服务**，降低耦合。
- **LLM 只负责它擅长的部分**，例如改写、仲裁、function calling、闲聊兜底。
- **本地模型负责高频稳定判断**，例如意图分类、拒识分类。
- **异步消费者只处理日志落盘**，这是典型的“把非关键路径剥离出去”的容器化改造思路。

## 3. 目录与职责划分

### 3.1 根目录

- `start.py`
  入口服务，项目最核心的编排文件。
- `async_consumer.py`
  RabbitMQ 消费者，负责把异步日志写入 `log/async_tasks.log`。
- `docker-compose.yml`
  定义整套服务编排关系。
- `README.md`
  说明了项目的 7 天改造目标和交付物。
- `.env` / `.env.example`
  放外部 API、服务地址、MQ、地图接口等配置。

### 3.2 `client/`

这个目录不是“前端客户端”，而是 **入口服务访问下游能力的调用封装层**：

- `nlu.py`
  调用 `chatnlu-service`。
- `reject.py`
  调用 `reject-service`。
- `arbitration.py`
  调用外部 LLM 做任务/FAQ/闲聊仲裁。
- `rewrite.py`
  调用外部 LLM 进行 query 改写。
- `correlation.py`
  用 LLM 判断当前 query 与上一轮请求是否相关。
- `stream_chat.py`
  调用外部 Bot 做闲聊流式输出。

这一层的设计价值是把入口服务里的“业务决策”和“HTTP 调用细节”分开。

### 3.3 `train/`

这个目录包含传统 NLP 分类模型部分：

- `intent_infer.py`
  加载本地意图分类模型，对输入做 TopK 意图预测。
- `reject_infer.py`
  加载本地拒识模型，输出是否拒识。
- `models/`
  BERT / BERT tiny 结构定义。
- `pretrained/`
  预训练权重。
- `saved/`
  训练后的分类模型 checkpoint。
- `data/`
  训练和验证数据。

这说明项目采用的是 **“本地分类模型 + 外部 LLM” 混合架构**，不是完全依赖在线大模型。

### 3.4 `function_call/`

这个目录是 NLU 语义理解层：

- `chatnlu_infer.py`
  先做意图召回，再用 LLM 的 function calling 做槽位抽取。
- `function.py`
  定义工具函数 schema，给 LLM 提供可调用能力描述。
- `slot_process.py`
  负责把 function calling 结果映射成系统内部的意图-槽位格式。
- `dm/`
  对天气、音乐、地图等具体 domain 做后处理。

这一层体现出项目的关键设计：

- 先用意图分类模型缩小候选范围；
- 再把候选工具列表喂给 LLM；
- 最后把 LLM 输出映射回内部统一结构。

这样比“把所有工具全集直接喂给 LLM”更稳，也更省 token。

### 3.5 `utils/`

- `redis_tool.py`
  Redis 连接与简单读写封装。
- `mq.py`
  RabbitMQ 生产者。
- `observability.py`
  统一生成 `trace_id/service/latency_ms/status` 结构化日志。
- `logger.py`
  项目日志基础设施。

### 3.6 `test/`

测试目录覆盖了几类场景：

- API 回归测试；
- 异步链路测试；
- 压测脚本；
- benchmark 脚本；
- 测试数据与结果样本。

这说明它不仅想“能跑”，还试图把容器化改造后的链路验证补齐。

## 4. 核心请求链路

最关键的文件是 `start.py`。它的工作不是自己做 NLU，而是作为一个 **对话路由与编排器**。

### 4.1 入口接入

入口通过 SocketIO 接收 `request_nlu` 事件，输入中主要包含：

- `query`
- `sender_id`
- `trace_id`
- `enable_dm`

收到请求后，会先构造一个 NLU 模板对象，后面不论走技能还是兜底都会基于这个模板返回。

### 4.2 读取会话状态

入口先从 Redis 读取上一轮状态：

- 上一轮所属域 `last_domain`
- 上一轮 query `last_query`
- 上一轮拒识结果 `last_reject`
- 上一轮回答 `last_answer`

这一步说明系统是有 **短期对话状态记忆** 的，而不是纯单轮推理。

### 4.3 Query 改写

`client/rewrite.py` 会根据 Redis 中保存的历史对话，调用外部 LLM 判断当前 query 是否需要改写。

设计意图：

- 用户多轮对话经常会出现省略主语、代词引用、上下文承接；
- 先改写成更完整的 query，后面的意图识别和槽位抽取会更稳定。

### 4.4 并发触发多个分支

`start.py` 用 `ThreadPoolExecutor` 并发提交多个任务：

- `request_nlu`
- `request_arbitration`
- `request_reject`
- `request_correlation`
- `request_chat`

这是这个项目非常重要的工程点：**不是串行一个个等结果，而是并发起任务，再根据仲裁结果决定取哪个分支的结果**。

优点：

- 缩短总响应时间；
- 闲聊/拒识/NLU 等结果可以提前准备；
- 入口层只做组合，不阻塞在单条子链路上。

### 4.5 仲裁优先决定大方向

`client/arbitration.py` 使用外部 LLM 和短期历史，把请求分成：

- `task`
- `faq`
- `chat`

当前主逻辑里，`faq` 实际没有独立服务，而是和 `chat/task` 分支一起被归并处理。核心决策是：

- 如果仲裁结果是 `task`，优先走 NLU 技能链路；
- 否则走拒识与闲聊兜底链路。

也就是说，这里的仲裁本质是在先做一个 **粗粒度路由**。

### 4.6 技能链路

如果判定为 `task`：

1. 读取 `chatnlu-service` 返回结果；
2. 如果 `function != Unknown`，说明成功命中一个技能；
3. 把结果直接回发给客户端；
4. 同时把 `SKILL#query#1#` 写入 Redis，记录最近一次服务状态。

这里的返回结果包含：

- `intent`
- `intent_id`
- `function`
- `slots`
- 可选的 `tool` / `nlg`

说明项目内部最终交付的是一个 **结构化动作语义**，而不是一段自然语言答案。

### 4.7 拒识与闲聊兜底链路

如果仲裁不是 `task`：

1. 先看拒识模型结果；
2. 如果拒识模型认为不该通过，再用相关性模型补判一次；
3. 如果仍然拒识，则返回拒识结果；
4. 否则调用闲聊 Bot 流式输出，分帧返回给客户端。

这里体现了一个典型思路：

- 拒识模型用于快速过滤；
- 相关性模型用于减少“本来相关却被误拒”的问题；
- 闲聊 Bot 作为最终兜底，保证系统始终“有话可说”。

### 4.8 异步日志链路

主链路结束后，入口服务会调用 `publish_async_log()`：

- 把 `trace_id/sender_id/query/status/latency/service` 写入 RabbitMQ；
- `async_consumer.py` 从队列消费消息；
- 最终把内容落盘到 `log/async_tasks.log`。

这样做的目的不是功能必需，而是工程优化：

- 主请求线程不用等待文件 IO；
- 即使日志处理出问题，主链路也不一定失败；
- 便于将来替换成更正式的审计/埋点/日志平台。

## 5. NLU 服务的构造思路

`function_call/chatnlu_infer.py` 是项目第二个最关键的文件。

它不是直接把 query 丢给大模型，而是做了三段式设计。

### 5.1 第一步：意图召回

先调用 `intent-service`，拿到 TopK 意图候选。

原因：

- 直接让大模型在所有技能上做 function calling，成本高且容易误召回；
- 用本地分类模型先缩小候选，可以控制工具范围；
- TopK 召回为后续 LLM 保留一定容错空间。

### 5.2 第二步：构造候选工具集

根据召回的意图 id，从 `config/class.txt`、`config/slot_intent.json` 和 `function.py` 中取出对应工具 schema。

这个设计说明：

- 意图标签和函数 schema 是映射关系，不是硬编码在一个地方；
- 槽位名和内部参数名还做了一层转换，方便对外 prompt 和对内执行解耦。

### 5.3 第三步：LLM function calling 抽槽

把缩小后的工具列表发给大模型做 function calling，再通过 `slot_process.intent_slot()` 转成统一字符串格式，最后再组装成结构化响应。

这是一种很常见、也比较实用的模式：

- 小模型做粗筛；
- 大模型做精抽；
- 本地规则做格式归一化。

## 6. 状态管理思路

Redis 在这个项目里不只是缓存，更像一个 **短期会话状态仓库**。

它主要存这些信息：

- `voice:last_service:{sender_id}`
  最近一次命中的服务类型、query、拒识状态、回答内容。
- `voice:rewrite_history:{sender_id}`
  改写链路历史。
- `voice:chat_history:{sender_id}`
  闲聊历史。
- `voice:arbitration_history:{sender_id}`
  仲裁历史。

这说明作者没有做数据库级长期记忆，而是只保留几十秒 TTL 的短期上下文。

优点：

- 简单；
- 足够支撑会话连续性；
- 不引入复杂数据表和存储设计。

缺点：

- TTL 很短，跨时段记忆丢失；
- 状态内容是字符串拼接格式，可维护性一般；
- 缺少更清晰的 session schema。

## 7. 容器化改造思路

从 `docker-compose.yml` 和 README 看，这个项目很明显做过一次“面向交付/面试展示”的容器化改造。

改造重点不是重写算法，而是补齐工程能力：

- 把每个服务拆成独立镜像；
- 用 Compose 管理依赖关系；
- 所有服务加 `/health` 健康检查；
- 使用 `restart: unless-stopped` 增强恢复能力；
- 引入 RabbitMQ 支撑异步链路；
- 把日志目录挂载到宿主机；
- 提供 GPU 可选配置。

这套改造非常适合拿来说明“如何把一个原始 AI demo 变成可部署系统”。

## 8. 为什么这样设计

从代码结构可以看出，作者想解决的是几个现实问题。

### 8.1 既要准确率，也要成本可控

如果所有事情都交给大模型：

- 成本高；
- 延迟大；
- 稳定性难控。

所以项目把任务拆成：

- 本地模型处理意图/拒识；
- LLM 处理改写、仲裁、相关性、function calling、闲聊。

这是一种典型的 **混合式 Agent 架构**。

### 8.2 既要多轮上下文，又不想引入复杂存储

所以作者选择了 Redis + TTL：

- 比数据库轻；
- 适合临时上下文；
- 满足任务型对话的短程依赖。

### 8.3 既要快速响应，又要保留多条备选分支

所以入口服务采用并发预取：

- NLU、闲聊、拒识、相关性同时起；
- 最终由仲裁结果决定主路径。

这不是最省资源的方案，但对“响应时间优先”的系统很有效。

### 8.4 既要工程可讲述性，又要交付可演示

所以项目加入了：

- Dockerfile
- Compose
- 健康检查
- 异步消费者
- 测试脚本
- 压测脚本
- 测试报告模板

这让它更像一个“完整项目作品”，而不只是模型推理脚本集合。

## 9. 优点总结

这个项目的构造优点比较明确：

- 服务职责拆分清楚，入口层和推理层分离；
- 本地分类模型 + LLM 的混合架构比较务实；
- Redis 让多轮对话具备短期上下文能力；
- RabbitMQ 把日志落盘从主链路解耦；
- Compose、healthcheck、restart 等工程化补得比较完整；
- 流式闲聊输出让用户体验比一次性返回更自然；
- `trace_id` 和结构化日志设计，便于联调和排障。

## 10. 可改进点

如果从“进一步工程化”的角度看，还有一些明显可以继续优化的地方。

### 10.1 入口服务承担的编排逻辑偏重

`start.py` 已经集成了：

- 会话读取；
- query 改写；
- 并发调度；
- 路由决策；
- 流式回传；
- 日志投递。

后续如果继续扩展，建议拆成：

- session manager
- orchestrator
- fallback handler
- response emitter

否则入口层会继续膨胀。

### 10.2 Redis 状态格式较脆弱

例如 `SKILL#{query}#1#` 这种字符串拼接方式，可读性和可扩展性一般。更稳妥的方式是统一存 JSON。

### 10.3 MQ 生产者每次发布都新建连接

`utils/mq.py` 当前是每次 `publish()` 都创建并关闭一次 RabbitMQ 连接。流量上来后，这会带来额外开销。更适合改成连接复用或连接池。

### 10.4 错误处理偏宽泛

不少地方直接 `except:`，这会隐藏真实错误来源。更适合改成：

- 捕获明确异常；
- 保留错误上下文；
- 区分可恢复错误和不可恢复错误。

### 10.5 FAQ 分支还不够完整

仲裁结果里有 `faq`，但当前整体链路里没有一个明显独立的 FAQ 服务或专门处理路径，说明这个分支更像预留位。

## 11. 一句话总结

这个项目的构造思路可以概括为：

> 用入口服务做对话编排，用本地分类模型做高频稳定判断，用大模型做改写/仲裁/抽槽/闲聊，用 Redis 管短期上下文，用 RabbitMQ 剥离非核心日志链路，再通过 Docker Compose 把整条任务型对话链路工程化交付出来。

如果你是用于面试表达，可以把它总结成三层：

1. **能力层**：意图、拒识、槽位抽取、闲聊、相关性判断。
2. **编排层**：入口服务并发调度、路径仲裁、状态管理。
3. **工程层**：容器化部署、健康检查、异步日志、测试与压测。

## 12. 关键源码入口

如果后面你要继续深挖，建议按这个顺序看：

1. `start.py`
2. `function_call/chatnlu_infer.py`
3. `client/arbitration.py`
4. `client/rewrite.py`
5. `client/stream_chat.py`
6. `train/intent_infer.py`
7. `train/reject_infer.py`
8. `docker-compose.yml`
9. `async_consumer.py`

这样最容易把“业务链路”和“工程链路”一起串起来。
